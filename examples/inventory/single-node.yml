# Single-node RKE2 inventory
# One server node — no HA, no VIP required

all:
  children:
    server_nodes:
      hosts:
        rke2-server:
          ansible_host: 10.0.0.10
  vars:
    ansible_user: root
    ansible_ssh_private_key_file: ~/.ssh/id_rsa
