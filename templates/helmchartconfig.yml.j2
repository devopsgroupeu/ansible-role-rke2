# Managed by Ansible - ansible-role-rke2
---
apiVersion: helm.cattle.io/v1
kind: HelmChartConfig
metadata:
  name: {{ item.name }}
  namespace: kube-system
spec:
  valuesContent: |-
{{ item.valuesContent | indent(4, true) }}
